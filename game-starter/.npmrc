# Force exact versions
save-exact=true

# Accept legacy peer deps
legacy-peer-deps=true

# Avoid engine restrictions
engine-strict=false

# Disable package-lock for Railway deployment
package-lock=false

# Set the registry to use NPM
registry=https://registry.npmjs.org/

# Increase network timeout for slow connections
network-timeout=60000 